<?xml version="1.0" encoding="UTF-8"?>
<!-- 映射文件DTO约束头 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" " http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 配置mapper标签,namespace属性，作用：唯一的 标识一个Mapper配置文件，保护文件中的标签ID不被污染。注意值不能重复 -->
<mapper namespace="com.shubao.mapper.ProductMapper">

    <select id="findAll" resultType="com.shubao.pojo.Product">
        select * from administrator.Producttb
    </select>

    <insert id="insertProduct" parameterType="com.shubao.pojo.Product">
        insert into administrator.Producttb (snap_dt, ptid, ptname, pdid, pdname, num)
        values (#{snap_dt}, #{ptid}, #{ptname}, #{pdid}, #{pdname}, #{num})
    </insert>

</mapper>